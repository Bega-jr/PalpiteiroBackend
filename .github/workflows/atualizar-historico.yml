name: Atualizar histórico Lotofácil

on:
  schedule:
    - cron: "30 0 * * *"  # 21:30 Brasil (00:30 UTC)
  workflow_dispatch:

jobs:
  atualizar:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: 📥 Checkout do repositório
        uses: actions/checkout@v4

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: 📦 Instalar dependências
        run: |
          pip install pandas requests

      - name: 🔄 Atualizar histórico
        run: python scripts/atualizar_historico.py

      - name: 📤 Commit automático se houver mudanças
        run: |
          git config --local user.email "github-actions@github.com"
          git config --local user.name "github-actions[bot]"

          if [ -f "data/historico_lotofacil.csv" ]; then
            git add data/historico_lotofacil.csv
            git diff --cached --quiet || git commit -m "Atualiza histórico Lotofácil (auto)"
          else
            echo "Arquivo CSV não encontrado."
          fi

      - name: 🚀 Push
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: main

