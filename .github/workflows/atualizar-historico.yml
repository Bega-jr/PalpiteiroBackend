name: Atualizar histórico Lotofácil

on:
  schedule:
    # 00:15 UTC equivale a 21:15 Horário de Brasília (UTC-3)
    - cron: "15 0 * * *"
  workflow_dispatch:

jobs:
  atualizar:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: 📥 Checkout do repositório
        uses: actions/checkout@v4

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: 📦 Instalar dependências
        run: |
          pip install requests pandas

      - name: 🔄 Atualizar histórico
        run: python scripts/atualizar_lotofacil.py

      - name: 📤 Commit automático se houver mudanças
        run: |
          git config user.email "github-actions@github.com"
          git config user.name "github-actions"

          git add data/Lotofacil.csv
          git diff --cached --quiet || git commit -m "Atualiza histórico Lotofácil"

      - name: 🚀 Push para o repositório
        run: git push origin main
